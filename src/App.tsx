import * as React from 'react';
import * as ReactDOM from 'react-dom';

const root = document.getElementById('root');

function render() {
  ReactDOM.render(<h2>Hello from React!</h2>, root);
}

render();
